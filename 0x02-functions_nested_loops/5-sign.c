#include "main.h"
/**
 * print_sign -entry point
 * @n: numbers to check
 * Return: 0 or 1
*/
int print_sign(int n)
{
	int numz;
        
        if (n > 0)
	{
	 value = 1;
         _putchar('+');
        }
       else if (n == 0)
       {
        value = 0;
        _putchar('0');
       }
       else
       {
	value = -1;
        _putchar('-')
       }
       
       return (numz);
}       
